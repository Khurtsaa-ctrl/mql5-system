# ҮҮЛЭН СЕРВЕР ДЭЭР ТАВИХ — АЛХАМ АЛХМААР

Дууссаны дараа: **компьютер унтарсан ч 15 минут тутам өөрөө шалгаж,
сайн ажил гармагц утас руу чинь мэдэгдэнэ.** Үнэгүй, карт шаардахгүй.

Нийт 20 минут.

---

## 1. GitHub бүртгэл (2 мин)

`github.com` → Sign up. Имэйл, нууц үг. Карт шаардахгүй.

Аль хэдийн бүртгэлтэй бол нэвтрээд 2-р алхам руу.

---

## 2. Repository үүсгэх (2 мин)

1. Баруун дээд булан `+` → **New repository**
2. Repository name: `mql5-system`
3. **Private** сонго (чухал — кодыг хэн ч харахгүй)
4. **Create repository**

---

## 3. Файлуудаа байршуулах (5 мин)

Шинэ хуудсан дээр **uploading an existing file** гэсэн холбоос дар.

Дараах файлуудыг чирж оруул:

```
agent.py
claude_api.py
cost_guard.py
db.py
job_finder.py
tg.py
triage.py
watch_once.py
```

**Commit changes** дар.

### Workflow файл нэмэх

Энэ нь тусгай хавтсанд орох ёстой.

1. **Add file** → **Create new file**
2. Нэрний талбарт яг ингэж бич:
   ```
   .github/workflows/mql5.yml
   ```
   (налуу зураас бичихэд хавтас өөрөө үүснэ)
3. `mql5.yml` файлын агуулгыг хуулж буулга
4. **Commit changes**

---

## 4. Нууц түлхүүрүүдээ хадгалах (5 мин)

Repository дотор **Settings** → зүүн талаас **Secrets and variables** →
**Actions** → **New repository secret**

Гурван удаа давтана:

| Name | Secret |
|---|---|
| `ANTHROPIC_API_KEY` | `sk-ant-...` (`~/.mql5_api_key` файлаас) |
| `TELEGRAM_TOKEN` | ботын токен |
| `TELEGRAM_CHAT` | чиний chat id |

Telegram-ийн хоёрыг компьютер дээрээс олох:

```powershell
type "$env:USERPROFILE\.mql5_ajil_cfg.json"
```

`token` ба `chat` гэсэн утгуудыг хуулна.

API түлхүүрээ олох:

```powershell
type "$env:USERPROFILE\.mql5_api_key"
```

---

## 5. Ажиллуулж шалгах (3 мин)

1. Дээд цэсний **Actions** таб
2. Зүүн талаас **MQL5 ajil hyanagch**
3. Баруун талд **Run workflow** → ногоон товч

1-2 минутын дараа ажиллана. Ногоон ✅ гарвал болсон.

Улаан ❌ гарвал дээр нь дарж алдааг харна — ихэвчлэн secret-ийн нэр
буруу бичигдсэн байдаг.

---

## 6. Дууссан

Одооноос:

- 15 минут тутам өөрөө шалгана
- Компьютер унтарсан ч ажиллана
- Шинэ сайн ажил гармагц утас руу хоёр мессеж ирнэ:
  монгол тайлбар, дараа нь хуулж болох англи санал
- Утаснаасаа MQL5 апп руу буулгаад Send

`START.bat` хэрэггүй болно. Компьютер дээрээ ажиллуулах шаардлагагүй.

---

## Санамжууд

**Хуваарь яг таг биш.** GitHub Actions заримдаа 15 минутын оронд
20-25 минут болдог. Ачаалалтай үед хойшилдог.

**Үнэгүй хязгаар.** Private repo сард 2000 минут үнэгүй. Нэг ажиллагаа
1-2 минут × өдөрт 96 удаа = сард ~4000 минут. Хэтэрвэл `cron`-ыг
`*/30` болгож 30 минут тутам болго — эсвэл repo-г **Public** болгвол
хязгааргүй үнэгүй (гэхдээ код ил харагдана, нууц түлхүүрүүд ил
харагдахгүй).

**Мэдээллийн сан.** GitHub-ийн cache дээр хадгалагдана. Заримдаа
арилдаг — тэр үед өмнө үзсэн ажлуудыг дахин үзэж, давхар мэдэгдэл
ирж болно. Эрсдэл бага.

**Төсөв.** `cost_guard` серверт ч ажиллана. $20 хязгаар хэвээр.

**Зогсоох.** Actions таб → workflow → `...` → **Disable workflow**
